import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sidemenu',
  webDir: '../../dist/apps/sidemenu',
  bundledWebRuntime: false,
};

export default config;
